# Scrolling King

ひたすら空の上を目指し、到達高度を競う縦スクロールゲームです。

## 遊び方

- 左右キー、または `A` / `D` で王様を移動
- 足場に着地すると自動でジャンプ
- 100mごとの高度ラインを越えて最高記録を更新
- スマートフォンでは画面下部の左右ボタンで操作

最高記録はブラウザに保存されます。

## ローカルで起動

```bash
npm install
npm run dev
```

本番用ビルド:

```bash
npm run build
```
